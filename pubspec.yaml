name: philiprehberger_sync_engine
description: >-
  Offline-first data sync with conflict resolution, retry queues, and local caching
version: 0.4.0
homepage: https://philiprehberger.com/open-source-packages/dart/philiprehberger_sync_engine
repository: https://github.com/philiprehberger/dart-sync-engine
issue_tracker: https://github.com/philiprehberger/dart-sync-engine/issues
topics:
  - sync
  - offline
  - cache

environment:
  sdk: ^3.6.0

dev_dependencies:
  test: ^1.25.0
  lints: ">=5.0.0 <7.0.0"
